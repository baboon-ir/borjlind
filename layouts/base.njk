<!doctype html>
<html lang="sv">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ title or "Rolf Börjlind" }}</title>
    <meta name="description" content="{{ teaser or description or "Rolf Börjlind – biografi" }}" />
    <link rel="stylesheet" href="/assets/css/main.css" />
  </head>
  <body class="min-h-screen font-serif {{ bodyClass or 'bg-dark text-light' }}">
    {% include "header.njk" %}

    <main class="{{ mainClass or 'mx-auto max-w-5xl px-4 py-10' }}">
      {{ content | safe }}
    </main>

    {% include "footer.njk" %}

    <script src="/assets/js/bio-reader.js" defer></script>
  </body>
</html>
