---
phase: 03-design-polish
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified: []
autonomous: false
requirements:
  - DESIGN-01
  - DESIGN-02
  - DESIGN-03
  - DESIGN-04
  - DESIGN-05
  - DESIGN-06
  - DESIGN-07
must_haves:
  truths:
    - "Reader design polish is verified in built output on both desktop and mobile breakpoints"
    - "Body text contrast remains readable against the selected warm-charcoal background"
    - "Text-spacing override resilience is validated with no clipping/overlap"
    - "Single-theme behavior remains intact and no theme toggle appears"
    - "Typography/readability rails (size/leading/tracking/measure) are confirmed in computed styles"
  artifacts:
    - path: "_site/biografi/index.html"
      provides: "Built biography reader used for final design verification"
      contains: "rb-bio-reader-wrap"
  key_links:
    - from: "assets/css/main.css design tokens and prose/chrome rules"
      to: "_site/biografi/index.html rendered reader"
      via: "Eleventy build output used for final acceptance"
      pattern: "rb-page-prose|rb-reader-kicker|rb-reader-footer"
---

<objective>
Verify and sign off Phase 3 design polish against readability and visual acceptance criteria using automated checks plus a blocking human verification checkpoint.

Purpose: Phase 3 is visual quality work; final acceptance requires human validation on real rendering surfaces in addition to build checks.
Output: Approved Phase 3 polish with evidence that all DESIGN requirements are satisfied.
</objective>

<execution_context>
@/Users/hakanfilip/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hakanfilip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-design-polish/03-CONTEXT.md
@.planning/phases/03-design-polish/03-RESEARCH.md
@.planning/phases/03-design-polish/03-01-SUMMARY.md
@assets/css/main.css
@layouts/biography.njk
@includes/bio-toc.njk
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated build and static polish assertions</name>
  <files>assets/css/main.css, layouts/biography.njk, includes/bio-toc.njk, assets/js/bio-reader.js</files>
  <action>
    Run build and static checks before manual review:

    ```bash
    cd /Users/hakanfilip/my-workspace/projects/borjlind
    npm run build

    # Token + typography rails
    rg -n "--rb-bg|--rb-text|--rb-body-size|--rb-body-leading|--rb-body-tracking|--rb-measure" assets/css/main.css

    # Symmetric logical insets and measure mapping
    rg -n "padding-inline|var\(--rb-inline-inset-mobile\)|var\(--rb-inline-inset-desktop\)|var\(--rb-measure\)" assets/css/main.css

    # Guard against introducing theme toggle surface
    rg -n "theme-toggle|dark-mode|light-mode|data-theme" layouts includes assets/js
    ```

    Fail the task if build fails or required rails are missing.
  </action>
  <verify>
    <automated>cd /Users/hakanfilip/my-workspace/projects/borjlind && npm run build && rg -n "--rb-body-size|--rb-body-leading|--rb-body-tracking|--rb-measure" assets/css/main.css</automated>
  </verify>
  <done>
    - Build exits 0
    - Required design rails exist in CSS
    - No theme toggle implementation appears
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual visual/readability verification (desktop + mobile)</name>
  <action>Pause for human sign-off. Validate visual/readability outcomes in browser using built site and dev server.</action>
  <verify>
    <automated>MISSING — manual verification required; see checklist below</automated>
  </verify>
  <done>User types "approved" after checklist is complete and acceptable.</done>
  <how-to-verify>
    Start server:
    - `cd /Users/hakanfilip/my-workspace/projects/borjlind && npx eleventy --serve`
    - Open `http://localhost:8080/biografi/`

    Desktop checks:
    1. Confirm reader background appears warm charcoal (not pure black) and body text appears warm off-white (not pure white).
    2. Confirm no dark/light toggle exists in reader UI.
    3. Inspect prose paragraph computed styles: body size ~17px, line-height ~1.72, letter-spacing slight positive.
    4. Confirm long-form measure visually stays constrained (about 65ch) on desktop width.
    5. Confirm kicker/footer/TOC panel share cohesive subdued surface tone and do not overpower prose.

    Mobile checks (responsive mode or real phone):
    6. Confirm left and right text insets are symmetric in paragraph flow.
    7. Confirm headings, figures, quotes, and details blocks align to the same horizontal inset rhythm.
    8. Confirm no clipping/overlap when applying WCAG text-spacing overrides in browser devtools:
       - line-height: 1.5
       - letter-spacing: 0.12em
       - word-spacing: 0.16em
       - paragraph spacing: 2em after paragraphs

    Final gate:
    9. Confirm reading experience feels calmer and more book-like while existing TOC/scroll behavior still works.
  </how-to-verify>
  <resume-signal>Type "approved" when all checks pass, or list issues found.</resume-signal>
</task>

</tasks>

<verification>
Automated checks from Task 1 plus manual checklist in Task 2.
</verification>

<success_criteria>
- `npm run build` exits 0
- Required design rails are present in CSS and reflected in rendered page
- No theme toggle is present (single-theme requirement intact)
- Desktop + mobile manual checks pass
- User approval received
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-polish/03-02-SUMMARY.md`
</output>
