---
phase: 02-ebook-reader
plan: "06"
type: execute
wave: 3
depends_on:
  - "02-03"
  - "02-04"
  - "02-05"
files_modified: []
autonomous: false
requirements:
  - READER-02
  - READER-03
  - READER-04
  - READER-05
  - READER-06
  - READER-07
  - NAV-03
  - NAV-04
  - LAYOUT-01
  - LAYOUT-02
must_haves:
  truths:
    - "Swiping left advances one page, swiping right retreats one page on a real mobile device"
    - "Swiping from the left screen edge does NOT trigger browser back navigation on iOS Safari"
    - "Arrow buttons and keyboard arrow keys change pages on desktop"
    - "The page indicator shows 'N / 276' and updates on every navigation"
    - "Closing and reopening the browser returns to the last page read"
    - "The TOC panel opens, lists all year periods, and jumping to one lands on the correct page"
    - "The year badge in the controls bar updates when crossing year group boundaries"
    - "A page with mediaPage: true displays its media centered with no overflow"
    - "Expanding a details accordion on a long page allows scrolling within the page"
  artifacts:
    - path: "_site/biografi/index.html"
      provides: "Fully built reader HTML with all reader elements present"
      contains: "rb-bio-reader-wrap"
  key_links:
    - from: "User swipe on mobile"
      to: "page visible changes"
      via: "bio-reader.js pointerup handler calls showPage"
      pattern: "manual verification"
    - from: "iOS left-edge swipe"
      to: "no browser back navigation"
      via: "20px EDGE_PX guard in pointerdown"
      pattern: "manual verification"
---

<objective>
Human verification of the complete e-book reader. All Wave 2 plans must be complete before this checkpoint. Claude runs the automated suite first, then the user tests on real devices.

Purpose: Several requirements (swipe, iOS edge-swipe, keyboard, localStorage, TOC, year badge, media pages, accordion scroll) can only be verified by a human using a browser — they depend on real touch hardware, browser history state, and visual inspection.

Output: Confirmed working reader on iOS Safari + desktop. Phase 2 ready for sign-off.
</objective>

<execution_context>
@/Users/hakanfilip/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hakanfilip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-ebook-reader/02-CONTEXT.md
@.planning/phases/02-ebook-reader/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification suite</name>
  <files></files>
  <action>
    Run all automated checks. Report results to the user before the manual checkpoint.

    ```bash
    # Full build
    cd /Users/hakanfilip/workspace/projects/private/borjlind
    npx eleventy

    # Validate build output (READER-06, NAV-02)
    node scripts/validate-build.js

    # READER-08: 100dvh on reader container
    grep -n "100dvh" assets/css/main.css

    # READER-09: no scroll model in bio-reader.js
    grep -c "scrollY\|scrollIntoView\|window\.scroll" assets/js/bio-reader.js

    # NAV-01: yearGroup in all biography .md files
    node scripts/check-frontmatter.js

    # NAV-02: year groups in built HTML
    grep -c "rb-year-groups" _site/biografi/index.html

    # TECH-01: no hardcoded 276 in templates or JS
    grep -rn '"276"\|= 276\|\/276' assets/js/bio-reader.js includes/ layouts/
    ```

    Expected results:
    - `npx eleventy` exits 0
    - `validate-build.js` exits 0, reports PASS for READER-06 and NAV-02
    - `grep -n "100dvh" assets/css/main.css` shows match on .rb-bio-reader-wrap
    - `grep -c "scrollY\|scrollIntoView\|window\.scroll" assets/js/bio-reader.js` returns 0
    - `check-frontmatter.js` reports equal counts
    - `grep -c "rb-year-groups" _site/biografi/index.html` returns at least 1
    - `grep -rn '"276"\|= 276\|\/276' ...` returns 0 matches

    If any check fails, fix the issue before proceeding to the manual checkpoint.
  </action>
  <verify>
    <automated>cd /Users/hakanfilip/workspace/projects/private/borjlind && npx eleventy 2>&1 | tail -3 && node scripts/validate-build.js</automated>
  </verify>
  <done>All automated checks pass. Automated suite is green before manual testing begins.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual device verification — swipe, keyboard, TOC, localStorage</name>
  <action>Pause for human to test the reader on real devices per the checklist below. All automated checks in Task 1 must be green before this step.</action>
  <verify>
    <automated>MISSING — manual verification required; see how-to-verify below</automated>
  </verify>
  <done>User types "approved" after completing the device checklist.</done>
  <what-built>
    Complete e-book reader for the 276-page biography. Wave 2 plans (02-03, 02-04, 02-05) have built:
    - Reader layout (100dvh container, paged model, no vertical scroll at container level)
    - New bio-reader.js with Pointer Events swipe, iOS edge-defense, keyboard navigation
    - Redesigned controls bar (prev/next buttons, "N / 276" indicator, year badge, TOC toggle)
    - TOC panel with year group jump buttons
    - localStorage position persistence (bio:page key)
    - LAYOUT-02 local scroll for expanded accordions
  </what-built>
  <how-to-verify>
    Start the development server: `cd /Users/hakanfilip/workspace/projects/private/borjlind && npx eleventy --serve`
    Open http://localhost:8080/biografi/ in your browser.

    **Desktop (Chrome or Safari):**
    1. Confirm one page is visible at a time — the biography content fills the screen, no vertical scroll of the full page list
    2. Click the left arrow button (←) — page retreats one page; indicator shows "N-1 / 276"
    3. Click the right arrow button (→) — page advances one page; indicator shows "N / 276"
    4. Press ArrowLeft and ArrowRight on your keyboard — pages change
    5. Navigate to page 30 (a different year group), confirm the year badge in the controls bar shows a different period than page 1
    6. Click the TOC button (☰) — the TOC panel opens and lists year groups
    7. Click any year group in the TOC — the reader jumps to the first page of that group; panel closes
    8. Navigate to page 50, close the tab, reopen http://localhost:8080/biografi/ — should return to page 50

    **iOS Safari (real device — not simulator):**
    9. Swipe right-to-left across the center of the screen — reader advances one page
    10. Swipe left-to-right across the center of the screen — reader retreats one page
    11. Swipe from the LEFT EDGE of the screen (within ~20px of edge) — browser must NOT navigate backward in history; reader stays on current page
    12. Verify the browser chrome (URL bar) is hidden correctly — reader fills the viewport with 100dvh

    **LAYOUT-02 (if a page with accordion content is available):**
    13. Find a biography page with a "Läs mer" / expandable details block
    14. Expand the accordion — page scrolls vertically within itself; the reader does not scroll at container level

    **If no media pages exist in the demo:** Skip LAYOUT-01 test (applies when full content is loaded).
  </how-to-verify>
  <resume-signal>Type "approved" when all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Manual device verification per checklist in how-to-verify above.
Automated suite must be green before manual testing begins (Task 1).
</verification>

<success_criteria>
- Automated suite exits 0 on all checks
- User types "approved" after completing the manual checklist
- iOS Safari swipe navigation works and edge-swipe does not trigger browser back
- Desktop arrow buttons and keyboard navigation work
- localStorage restores reading position on tab reopen
- TOC opens, lists year groups, and jumps correctly
- Phase 2 declared complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-ebook-reader/02-06-SUMMARY.md`
</output>
