---
phase: 01-css-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/main.css
autonomous: true
requirements:
  - CSS-02
  - CSS-03

must_haves:
  truths:
    - "A single assets/css/main.css file exists and is human-readable plain CSS with no @tailwind or @apply directives"
    - "The file declares CSS custom properties for all three brand colors and all three font stacks in :root"
    - "All component classes from tailwind.css are present with explicit property declarations"
    - "The site builds successfully with npx @11ty/eleventy after the file is written"
  artifacts:
    - path: "assets/css/main.css"
      provides: "Complete hand-written CSS replacing Tailwind compiled output"
      contains: ":root { --color-dark"
  key_links:
    - from: "assets/css/main.css"
      to: "layouts/base.njk"
      via: "passthrough copy in .eleventy.js, linked via <link> tag"
      pattern: "addPassthroughCopy.*main\\.css"
---

<objective>
Write the new hand-written `assets/css/main.css` that replaces the Tailwind-compiled output. This file must contain CSS custom properties (design tokens), a minimal reset replacing Tailwind preflight, and all component rules with every `@apply` directive expanded to explicit CSS property declarations.

Purpose: The new CSS file is the foundation of Phase 1 — it must exist before templates can be cleaned of Tailwind utility classes in Plan 02.
Output: `assets/css/main.css` — a complete, human-readable plain CSS file.
</objective>

<execution_context>
@/Users/hakanfilip/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hakanfilip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-css-foundation/01-RESEARCH.md

<!-- Key source files to read before writing -->
@assets/css/tailwind.css
</context>

<interfaces>
<!-- Resolved brand values from tailwind.config.cjs -->
<!-- These are the CSS custom properties the new file must declare -->

:root {
  color-scheme: dark;
  --header-h: 56px;

  /* Brand colors */
  --color-dark: #1c1d1e;
  --color-hub: #282828;
  --color-light: #fdf9f0;

  /* Font stacks */
  --font-serif: ui-serif, Georgia, Cambria, "Times New Roman", Times, serif;
  --font-sans: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, Inter, Arial, sans-serif;
  --font-mono: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace;
}

@media (min-width: 768px) {
  :root { --header-h: 98px; }
}

<!-- Eleventy passthrough (no change needed, already in .eleventy.js) -->
eleventyConfig.addPassthroughCopy({ "assets/css/main.css": "assets/css/main.css" });
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write assets/css/main.css — custom properties, reset, and all component rules</name>
  <files>assets/css/main.css</files>
  <action>
    Write a complete plain CSS file at `assets/css/main.css`. The current file is empty (0 bytes). Use `assets/css/tailwind.css` as the reference for which rules and selectors exist, and expand every `@apply` to explicit CSS declarations. Use the compiled property values documented in the RESEARCH.md @apply inventory table.

    The file must follow this exact section order:

    **Section 1 — Custom Properties**
    Declare `:root` with:
    - `color-scheme: dark`
    - `--header-h: 56px`
    - `--color-dark: #1c1d1e`, `--color-hub: #282828`, `--color-light: #fdf9f0`
    - `--font-serif`, `--font-sans`, `--font-mono` (full stacks as in RESEARCH.md)
    - Media query: `@media (min-width: 768px) { :root { --header-h: 98px; } }`

    **Section 2 — Minimal Reset (replaces Tailwind preflight)**
    Copy the exact reset block from RESEARCH.md "Don't Hand-Roll" section verbatim. This covers: box-sizing, margin/padding zero on html/body, heading font inheritance, paragraph/blockquote zero margin, anchor color inheritance, button/input font inheritance, media elements display:block, img/video max-width, list-style none, summary/textarea/hidden rules.

    **Section 3 — Base / Global**
    Expand `html, body { @apply font-serif bg-dark text-light; }`:
    ```css
    html, body {
      background-color: var(--color-dark);
      font-family: var(--font-serif);
      color: var(--color-light);
      font-size: 1.0625rem;
      line-height: 1.7;
      font-weight: 400;
    }
    body {
      background:
        linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.5)),
        url(/assets/images/noise-light.png) top left repeat;
    }
    @media (max-width: 640px) { body { font-size: 1rem; } }
    h1, h2, h3, h4, h5, h6 { line-height: 1.05; }
    .h-full { height: calc(100vh - var(--header-h)); }
    ```

    **Section 4 — Hub background components**
    Copy `.rb-hub-bg`, `.rb-hub-bg footer`, `.rb-hub-pattern`, `.rb-hub-video`, `.rb-hub-overlay` verbatim from tailwind.css (these have no `@apply` — already explicit). Replace `theme('colors.hub')` with `var(--color-hub)`.

    **Section 5 — Navigation**
    Copy `.rb-header`, `.rb-header.is-scrolled` verbatim (no `@apply`).
    `.rb-brand` verbatim.
    Expand `.rb-navlink { @apply no-underline text-light hover:text-light; }`:
    ```css
    .rb-navlink { text-decoration: none; color: var(--color-light); }
    .rb-navlink:hover { color: var(--color-light); }
    ```
    Expand `.rb-nav-toggle, .rb-nav-close { @apply text-light hover:text-light; }`:
    ```css
    .rb-nav-toggle, .rb-nav-close {
      color: var(--color-light);
      background: transparent;
      border: 0;
      padding: 0;
      font-size: 16px;
    }
    .rb-nav-toggle:hover, .rb-nav-close:hover { color: var(--color-light); }
    ```
    Copy `.rb-nav-icon`, `.rb-nav-open .rb-nav-icon` verbatim.
    Copy `.rb-nav-open [data-nav-panel] { display: block; }` verbatim.

    Add the nav panel component rules (replacing utilities used in header.njk — Plan 02 will add these class names to the template):
    ```css
    [data-nav-panel] {
      position: fixed;
      inset: 0;
      z-index: 50;
      display: none;
    }
    .rb-nav-backdrop {
      position: absolute;
      inset: 0;
      background-color: #000;
    }
    .rb-nav-content {
      position: relative;
      z-index: 10;
      display: flex;
      min-height: 100%;
      flex-direction: column;
      align-items: flex-start;
      justify-content: flex-start;
      padding: 140px 2rem 2rem;
      max-width: 64rem;
      margin: 0 auto;
    }
    .rb-nav-list {
      text-align: left;
      font-size: 1.875rem;
      padding: 0;
      margin: 0;
      list-style: none;
    }
    .rb-nav-list li + li { margin-top: 1.5rem; }
    .rb-nav-list a { display: block; }
    ```

    **Section 6 — Hero**
    Expand `.rb-hero-inner { @apply relative; }` → `position: relative;`
    Expand `.rb-hero-title { @apply text-center font-serif font-bold leading-[1.15]; }`:
    ```css
    .rb-hero-title {
      text-align: center;
      font-family: var(--font-serif);
      font-weight: 700;
      line-height: 1.15;
      font-size: clamp(44px, 6vw, 88px);
      color: #fdf9f0;
      opacity: 1;
    }
    ```
    Copy `.rb-hero { position: relative; z-index: 10; }` verbatim.

    **Section 7 — CTA and Cards**
    Expand `.rb-cta { @apply inline-flex items-center justify-center no-underline; }`:
    ```css
    .rb-cta {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      text-decoration: none;
      background-color: var(--color-light);
      color: var(--color-dark);
      font-size: 14px;
      padding: 4px 12px;
      border-radius: 6px;
      font-weight: 500;
      transition: opacity 0.2s ease;
    }
    .rb-cta:hover { opacity: 0.9; }
    ```
    Add CTA wrapper rule (Plan 02 replaces inline Tailwind on home.njk):
    ```css
    .rb-cta-wrap {
      display: flex;
      justify-content: center;
      margin-top: 2rem;
    }
    ```
    Expand `.rb-card { @apply block h-full rounded-2xl border-2 border-light/80 bg-transparent p-4 no-underline; }`:
    ```css
    .rb-card {
      display: block;
      height: 100%;
      border-radius: 1rem;
      border: 2px solid rgba(253,249,240,0.8);
      background: transparent;
      padding: 1rem;
      text-decoration: none;
      box-shadow: 0 0 0 1px rgba(255,255,255,0.04) inset;
    }
    .rb-card:hover { border-color: var(--color-light); }
    ```
    Expand `.rb-card-title { @apply text-sm font-semibold; }` → `font-size: 0.875rem; font-weight: 600;`
    Expand `.rb-card-sub { @apply mt-1 text-xs text-light/70; }` → `margin-top: 0.25rem; font-size: 0.75rem; color: rgba(253,249,240,0.7);`
    Add hub grid rule (Plan 02 replaces `mt-6 grid gap-4 sm:grid-cols-3` in hub-grid.njk):
    ```css
    .rb-hub-grid {
      margin-top: 1.5rem;
      display: grid;
      gap: 1rem;
      grid-template-columns: 1fr;
    }
    @media (min-width: 640px) {
      .rb-hub-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); }
    }
    ```

    **Section 8 — Biography title block**
    Expand `.rb-title-block { @apply mx-auto text-center; }`:
    ```css
    .rb-title-block {
      margin: 0 auto;
      text-align: center;
      height: calc(100vh - var(--header-h));
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      background-image: url('/assets/images/pattern.png');
      background-size: cover;
      background-position: center;
      position: relative;
    }
    ```
    Copy `.rb-title-block::before` and `::after` verbatim.
    Expand `.eleventy-plugin-youtube-embed { @apply my-24; }` → `margin-top: 6rem; margin-bottom: 6rem;`
    Expand `.rb-title-author { @apply text-base text-light; }` → `font-size: 1rem; color: var(--color-light);`
    Expand `.rb-title-line { @apply text-base text-light; }` → same.

    **Section 9 — Biography controls**
    Expand `.rb-controls { @apply bg-dark/90 text-light; }`:
    ```css
    .rb-controls {
      background-color: rgb(28 29 30 / 0.95);
      color: var(--color-light);
      -webkit-backdrop-filter: blur(8px);
      backdrop-filter: blur(8px);
    }
    ```
    Expand `.rb-page-input { @apply bg-transparent border-0 px-2 py-1 text-light text-center focus:outline-none; }`:
    ```css
    .rb-page-input {
      background: transparent;
      border: 0;
      padding: 0.25rem 0.5rem;
      color: var(--color-light);
      text-align: center;
      font-size: 32px;
      width: 5ch;
    }
    .rb-page-input:focus { outline: none; }
    ```
    Expand `.rb-input { @apply w-24 rounded-xl border-2 border-light/50 bg-dark/60 px-3 py-2 text-sm text-light focus:outline-none; }`:
    ```css
    .rb-input {
      width: 6rem;
      border-radius: 0.75rem;
      border: 2px solid rgba(253,249,240,0.5);
      background: rgba(28,29,30,0.6);
      padding: 0.5rem 0.75rem;
      font-size: 0.875rem;
      color: var(--color-light);
    }
    .rb-input:focus { outline: none; }
    ```
    Expand `.rb-btn { @apply inline-flex items-center justify-center rounded-xl border-2 border-light/70 bg-transparent px-4 py-2 text-sm text-light no-underline; }`:
    ```css
    .rb-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border-radius: 0.75rem;
      border: 2px solid rgba(253,249,240,0.7);
      background: transparent;
      padding: 0.5rem 1rem;
      font-size: 0.875rem;
      color: var(--color-light);
      text-decoration: none;
    }
    .rb-btn:hover { border-color: var(--color-light); }
    ```

    **Section 10 — Biography page**
    Expand `.rb-bio-page { @apply w-full bg-transparent; }`:
    ```css
    .rb-bio-page {
      width: 100%;
      background: transparent;
      max-width: 65ch;
      padding: 0;
      margin: 0 auto;
      content-visibility: auto;
      contain-intrinsic-size: auto 800px;
    }
    ```
    Expand `.rb-page-meta { @apply hidden; }` → `display: none;`
    Copy the full `@media (max-width: 768px)` `.rb-bio-page` block verbatim from tailwind.css (the large block with all the mobile overrides — lines 247–324).

    **Section 11 — Prose component (.rb-prose)**
    The `@apply prose prose-invert max-w-none` macro would expand to ~400 lines. Since `main.css` is currently empty (0 bytes — the Tailwind CLI has not run recently), derive the prose rules from the base Tailwind typography plugin output. Write the essential prose rules directly — the styles defined in `tailwind.css` below the `@apply` line ARE the project's custom overrides and must be kept:

    ```css
    .rb-prose {
      max-width: none;
      /* Tailwind typography base colors for prose-invert (dark background) */
      color: var(--color-light);
      line-height: 1.75;
    }
    /* Base prose element spacing */
    .rb-prose :where(p) { margin-top: 1.25em; margin-bottom: 1.25em; }
    .rb-prose :where(blockquote) { margin-top: 1.6em; margin-bottom: 1.6em; border-left-width: 0.25rem; padding-left: 1em; }
    .rb-prose :where(ul) { margin-top: 1.25em; margin-bottom: 1.25em; padding-left: 1.625em; list-style-type: disc; }
    .rb-prose :where(ol) { margin-top: 1.25em; margin-bottom: 1.25em; padding-left: 1.625em; list-style-type: decimal; }
    .rb-prose :where(li) { margin-top: 0.5em; margin-bottom: 0.5em; }
    .rb-prose :where(strong) { font-weight: 600; color: var(--color-light); }
    .rb-prose :where(em) { font-style: italic; }
    .rb-prose :where(code) { font-family: var(--font-mono); font-size: 0.875em; }
    .rb-prose :where(pre) { font-family: var(--font-mono); font-size: 0.875em; overflow-x: auto; padding: 0.857em 1.14em; }
    ```

    Then copy the project-specific overrides directly from tailwind.css (these come after the `@apply` line):
    ```css
    .rb-prose :where(h1,h2,h3,h4,h5,h6) {
      color: var(--color-light);
      font-size: 1.4rem;
      font-weight: 600;
      letter-spacing: 0.06em;
      margin-bottom: 3rem;
    }
    .rb-prose p { margin-bottom: 1.5em; color: var(--color-light); white-space: pre-wrap; }
    .rb-prose hr { height: 1px; border: 0; background-color: var(--color-light); margin: 0 0 1.5em; opacity: .9; }
    .rb-prose a { color: #4169e1; text-decoration: underline; }
    .rb-prose a:visited { color: #800080; }
    .rb-prose a:hover, .rb-prose a:focus, .rb-prose a:active { color: #191970; }
    ```
    Note: Replace `theme('colors.light')` with `var(--color-light)` in all three of the above rules.

    **Section 12 — Content components**
    Expand `.read-more { @apply inline-block mb-1 text-xs font-light text-white; }`:
    ```css
    .read-more {
      display: inline-block;
      margin-bottom: 0.25rem;
      font-size: 0.75rem;
      font-weight: 300;
      color: #ffffff;
    }
    ```
    Copy `.rb-accordion summary`, `.rb-accordion`, `.rb-accordion[open]` verbatim (replace `theme()` calls with variables).
    Expand `.rb-minne { @apply my-6 p-4; }`:
    ```css
    .rb-minne {
      margin-top: 1.5rem;
      margin-bottom: 1.5rem;
      padding: 1rem;
      background-color: rgb(28, 29, 30);
    }
    ```
    Expand `.rb-quote { @apply my-6 p-4 italic border-light pl-4; border-left: 0.5px solid; }`:
    ```css
    .rb-quote {
      margin-top: 1.5rem;
      margin-bottom: 1.5rem;
      padding: 1rem;
      font-style: italic;
      border-left: 0.5px solid var(--color-light);
      padding-left: 1rem;
    }
    ```
    Copy `.rb-prose figure`, `.rb-prose img` (expand `@apply shadow-md`), `.rb-prose figcaption` (expand `@apply mt-2 text-sm italic text-light/70`):
    ```css
    .rb-prose figure { margin: 4em 0; }
    .rb-prose img {
      box-shadow: 0 4px 6px -1px rgba(0,0,0,0.1), 0 2px 4px -2px rgba(0,0,0,0.1);
      display: block;
      margin-left: auto;
      margin-right: auto;
      max-width: min(450px, 100%);
      height: auto;
    }
    .rb-prose figcaption {
      margin-top: 0.5rem;
      font-size: 0.875rem;
      font-style: italic;
      color: rgba(253,249,240,0.7);
      text-align: center;
    }
    ```
    Expand `.rb-sep { @apply mx-auto my-24 text-center text-light/80 stars; }`:
    The compiled result merges `.stars` rules — the `.stars` `margin` (112px 0) overrides `my-24`:
    ```css
    .rb-sep {
      margin: 0 auto;
      text-align: center;
      color: rgba(253,249,240,0.8);
      font-size: 48px;
      font-weight: 900;
      margin-top: 112px;
      margin-bottom: 112px;
    }
    .stars {
      font-size: 48px;
      font-weight: 900;
      margin: 112px 0;
    }
    ```
    Add `.rb-part { margin-top: 12rem; margin-bottom: 6rem; }` (expand `@apply mt-48 mb-24`).

    **Section 13 — Markdown containers**
    Expand `.rb-center { @apply text-center my-6; }` → `text-align: center; margin-top: 1.5rem; margin-bottom: 1.5rem;`
    Expand `.rb-indent { @apply my-4; padding-left: 2em; }` → `margin-top: 1rem; margin-bottom: 1rem; padding-left: 2em;`
    Copy `.rb-poem::before, .rb-poem::after` verbatim.
    Expand `.rb-poem` (double `@apply` — `my-28` wins over `my-6`):
    ```css
    .rb-poem {
      margin-top: 7rem;
      margin-bottom: 7rem;
      font-style: italic;
      text-align: center;
    }
    ```
    Expand `.rb-poem p { @apply my-6; }` → `margin-top: 1.5rem; margin-bottom: 1.5rem;`
    Expand `.rb-video { @apply my-6 overflow-hidden rounded-2xl border border-zinc-800 bg-black/30; }`:
    ```css
    .rb-video {
      margin-top: 1.5rem;
      margin-bottom: 1.5rem;
      overflow: hidden;
      border-radius: 1rem;
      border: 1px solid rgb(39,39,42);
      background: rgba(0,0,0,0.3);
    }
    ```
    Expand `.rb-video iframe, .rb-video video { @apply w-full aspect-video; }`:
    ```css
    .rb-video iframe, .rb-video video { width: 100%; aspect-ratio: 16/9; }
    ```

    **Section 14 — YouTube embed (semantic class for .eleventy.js)**
    Add new semantic class (Plan 02 will update renderBio in .eleventy.js to use it):
    ```css
    .rb-yt-embed {
      margin-top: 1.25rem;
      margin-bottom: 1.25rem;
      overflow: hidden;
      border-radius: 1rem;
      border: 1px solid rgb(39,39,42);
      background: rgba(0,0,0,0.3);
    }
    .rb-yt-embed-ratio { aspect-ratio: 16/9; }
    .rb-yt-embed-ratio iframe { width: 100%; height: 100%; }
    ```

    **Section 15 — MORE accordion (semantic class for .eleventy.js)**
    Add semantic class for the `[MORE]` details block (Plan 02 updates renderBio):
    ```css
    .rb-more-block {
      margin-top: 1.5rem;
      border-radius: 1rem;
      border: 1px solid rgb(39,39,42);
      background: rgba(0,0,0,0.3);
      padding: 1rem;
    }
    .rb-more-summary {
      cursor: pointer;
      user-select: none;
      font-size: 0.875rem;
      color: rgb(212,212,216);
    }
    .rb-more-content { margin-top: 1rem; }
    ```

    **Section 16 — Appendix layout**
    Copy `.appendix-layout`, `.appendix-nav`, `.appendix-nav::-webkit-scrollbar`, `.appendix-nav ul` verbatim (replace `theme()` calls).
    Replace `color: theme('colors.light')` in `.appendix-nav-link` with `color: var(--color-light)`. Copy rest of `.appendix-nav-link` and `:hover` verbatim.
    Copy `.appendix-content`, `.appendix-content > *` verbatim.
    Expand `.appendix-content h1 { @apply text-3xl font-bold mb-6; }`:
    ```css
    .appendix-content h1 { font-size: 1.875rem; font-weight: 700; margin-bottom: 1.5rem; }
    ```
    Expand `.appendix-content h2 { @apply text-xl font-semibold mt-8 mb-4; border-bottom... }`:
    ```css
    .appendix-content h2 {
      font-size: 1.25rem;
      font-weight: 600;
      margin-top: 2rem;
      margin-bottom: 1rem;
      border-bottom: 1px solid rgba(253,249,240,0.2);
      padding-bottom: 0.5rem;
    }
    ```
    Expand `.appendix-content p { @apply mb-4 leading-relaxed; }` → `margin-bottom: 1rem; line-height: 1.625;`
    Copy `.memories h2` verbatim.

    **Section 17 — Header layout (semantic classes)**
    Add semantic classes replacing utilities used in header.njk (Plan 02 will update the template):
    ```css
    .rb-header-outer {
      position: sticky;
      top: 0;
      z-index: 100;
      color: var(--color-light);
    }
    .rb-header-inner {
      margin: 0 auto;
      display: flex;
      height: var(--header-h);
      max-width: 64rem;
      align-items: center;
      justify-content: space-between;
      gap: 1.5rem;
      padding: 0 1rem;
      position: relative;
    }
    .rb-header-nav {
      display: flex;
      align-items: center;
      gap: 1rem;
      font-size: 0.875rem;
    }
    .rb-nav-toggle-wrap {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    ```

    **Section 18 — Base layout (semantic classes)**
    Add semantic classes replacing utilities in base.njk (Plan 02 will update template):
    ```css
    .rb-body {
      min-height: 100vh;
      font-family: var(--font-serif);
      background-color: var(--color-dark);
      color: var(--color-light);
      display: flex;
      flex-direction: column;
    }
    .rb-main {
      flex: 1;
      position: relative;
      z-index: 10;
      width: 100%;
    }
    .rb-main-inner {
      margin: 0 auto;
      max-width: 64rem;
    }
    .rb-main-centered {
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .rb-mt-auto { margin-top: auto; }
    ```

    **Section 19 — bio-controls layout (semantic classes)**
    Add semantic classes for bio-controls.njk (Plan 02 will update the template):
    ```css
    .rb-controls-outer {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      z-index: 100;
    }
    .rb-controls-inner {
      margin: 0 auto;
      display: flex;
      height: var(--header-h);
      max-width: 64rem;
      align-items: center;
      justify-content: space-between;
      gap: 1.5rem;
      padding: 0 1rem;
    }
    .rb-controls-group {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    ```

    **Section 20 — bio-page layout (semantic classes)**
    Add semantic classes for bio-page.njk (Plan 02 will update template):
    ```css
    .rb-page-meta-text {
      font-size: 0.75rem;
      font-family: var(--font-mono);
      color: rgba(253,249,240,0.6);
    }
    .rb-page-meta-text a {
      text-decoration: none;
      font-size: 0.75rem;
      font-family: var(--font-mono);
      color: rgba(253,249,240,0.6);
    }
    .rb-page-placeholder {
      font-size: 0.875rem;
      color: rgba(253,249,240,0.6);
    }
    ```

    **Section 21 — Biography layout (semantic classes)**
    Add semantic class for biography.njk container (Plan 02 will update template):
    ```css
    .rb-bio-container {
      margin-top: 2.5rem;
      display: grid;
    }
    ```

    **Section 22 — Footer (semantic classes)**
    Add semantic classes for footer.njk (Plan 02 will update template):
    ```css
    .rb-footer {
      border-top: 1px solid rgb(24,24,27);
      padding: 2.5rem 0;
    }
    .rb-footer-inner {
      margin: 0 auto;
      max-width: 64rem;
      padding: 0 1rem;
      font-size: 0.75rem;
      color: rgb(113,113,122);
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: space-between;
      gap: 1rem;
    }
    ```

    **Section 23 — Appendix and Minnen layout (semantic classes)**
    Add semantic classes for appendix.njk and minnen.njk (Plan 02 will update templates):
    ```css
    .rb-page-title-wrap {
      margin: 0 auto;
      max-width: 64rem;
      padding: 0.5rem 1rem 1.5rem;
    }
    .rb-page-h1 {
      font-size: 3.75rem;
      font-weight: 700;
      margin-bottom: 1rem;
      text-transform: lowercase;
    }
    .rb-page-desc { font-size: 1.125rem; }
    .rb-page-content { padding: 0 1rem; }
    ```

    **Section 24 — Memory layout (semantic classes)**
    Add semantic classes for memory.njk (Plan 02 will update template):
    ```css
    .rb-memory-section {
      padding: 1.5rem;
    }
    .rb-memory-header {
      display: flex;
      flex-wrap: wrap;
      align-items: flex-start;
      justify-content: space-between;
      gap: 1rem;
    }
    .rb-memory-title {
      font-size: 1.25rem;
      font-weight: 600;
      letter-spacing: -0.025em;
    }
    .rb-memory-period {
      margin-top: 0.25rem;
      font-size: 0.75rem;
      font-family: var(--font-mono);
      color: rgb(113,113,122);
    }
    .rb-memory-teaser {
      margin-top: 1rem;
      font-size: 0.875rem;
      color: rgb(212,212,216);
    }
    .rb-memory-content { margin-top: 1.5rem; }
    ```

    **Section 25 — Mobile appendix override**
    Copy the `@media (max-width: 768px)` appendix block from the bottom of tailwind.css verbatim.

    Critical rules:
    - Every `theme('colors.X')` call must be replaced with `var(--color-X)`. There are 8 such calls total.
    - No `@apply` directive may appear in the output file.
    - No `@tailwind` directive may appear in the output file.
    - The `.rb-prose` section must NOT reference Tailwind `prose` or `prose-invert` in any way.
  </action>
  <verify>
    <automated>cd /Users/hakanfilip/workspace/projects/private/borjlind && grep -c "@apply\|@tailwind\|theme(" assets/css/main.css && echo "Expected: 0"</automated>
    <automated>cd /Users/hakanfilip/workspace/projects/private/borjlind && grep "color-scheme\|--color-dark\|--color-light\|--color-hub\|--font-serif\|--font-sans\|--font-mono" assets/css/main.css | wc -l && echo "Expected: 7+"</automated>
    <automated>cd /Users/hakanfilip/workspace/projects/private/borjlind && npx @11ty/eleventy 2>&1 | tail -5</automated>
  </verify>
  <done>
    assets/css/main.css exists with no @apply or @tailwind directives, no theme() calls, contains all CSS custom properties in :root, all component rules are present with explicit declarations, and npx @11ty/eleventy completes without error.
  </done>
</task>

</tasks>

<verification>
After task completion, confirm:
1. `grep -c "@apply\|@tailwind\|theme(" assets/css/main.css` returns 0
2. `:root` block contains all 3 colors (--color-dark, --color-hub, --color-light) and all 3 font stacks
3. Key selectors exist: .rb-prose, .rb-hub-bg, .rb-controls, .rb-card, .rb-btn, .rb-bio-page, .rb-sep, .rb-footer
4. `npx @11ty/eleventy` builds without error (the site still uses Tailwind in templates at this point — that's fine, Plan 02 fixes templates)
</verification>

<success_criteria>
- assets/css/main.css exists and contains no Tailwind directives
- All @apply directives from tailwind.css are expanded to explicit property declarations
- All theme() calls replaced with var(--color-X) references
- CSS custom properties declared for all 3 brand colors and all 3 font stacks
- Eleventy build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-css-foundation/01-01-SUMMARY.md` covering:
- What was written (sections and key decisions)
- Any deviations from the plan and why
- Exact selectors added beyond tailwind.css source (new semantic classes)
- Build status
</output>
