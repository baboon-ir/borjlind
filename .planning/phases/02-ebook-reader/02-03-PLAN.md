---
phase: 02-ebook-reader
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - assets/css/main.css
autonomous: true
requirements:
  - READER-01
  - READER-03
  - READER-08
  - LAYOUT-02
must_haves:
  truths:
    - "The biography reader occupies exactly the full viewport height with no vertical scroll at the container level"
    - "Inactive pages are hidden without taking up layout space"
    - "The active page can scroll internally when accordion content is expanded"
    - "The controls bar is fixed at the bottom and always visible"
    - "The TOC panel slides in from the right and covers the reader"
    - "iOS Safari receives the pan-y touch-action signal on the reader container"
  artifacts:
    - path: "assets/css/main.css"
      provides: "Reader layout, page visibility model, controls bar, TOC panel, LAYOUT-02 accordion scroll"
      contains: "100dvh"
  key_links:
    - from: "assets/css/main.css .rb-bio-reader-wrap"
      to: "height: 100dvh"
      via: "READER-08 container height"
      pattern: "100dvh"
    - from: "assets/css/main.css .rb-bio-page:not(.is-active)"
      to: "content-visibility: hidden"
      via: "READER-01 page visibility model"
      pattern: "content-visibility"
---

<objective>
Append all new reader CSS sections to assets/css/main.css. This covers the paged reader container layout, the page visibility model, the controls bar, the TOC panel, and the LAYOUT-02 accordion scroll fix.

Purpose: The CSS must be written before the reader JS (plan 02-04) and template wiring (plan 02-05) so that when they are applied the layout works immediately. This plan is parallel to 02-04 and 02-05 — it only modifies main.css.

Output: assets/css/main.css with five new CSS sections appended: reader wrapper, page visibility model, controls bar, TOC panel, media page layout.
</objective>

<execution_context>
@/Users/hakanfilip/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hakanfilip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-ebook-reader/02-CONTEXT.md
@.planning/phases/02-ebook-reader/02-RESEARCH.md
@.planning/phases/02-ebook-reader/02-01-SUMMARY.md

<interfaces>
<!-- Key existing CSS class names from main.css that new rules must coexist with -->

Existing classes referenced by new reader CSS:
- `.rb-bio-container` — currently a plain container div; will gain `touch-action: pan-y` and `overflow: hidden`
- `.rb-bio-page` — currently visible; will get the visibility model applied
- `.rb-controls-outer.rb-controls` — current controls wrapper; bio-controls.njk redesign (plan 02-05) replaces its content

New rb-* classes introduced by this plan (used in templates created by plan 02-05):
- `.rb-bio-reader-wrap` — wraps controls + container, height 100dvh
- `.rb-controls-bar` — redesigned bottom controls bar
- `.rb-ctrl-btn` — prev/next/TOC buttons
- `.rb-ctrl-center` — center group with indicator + year badge
- `.rb-page-indicator` — "N / 276" text
- `.rb-year-badge` — current year period label
- `.rb-toc-panel` — fixed overlay panel
- `.rb-toc-inner` — scrollable panel content
- `.rb-toc-list` — year group list
- `.rb-toc-item` — year group button
- `.rb-toc-close` — close button
- `.rb-bio-page--media` — media-dedicated page layout (LAYOUT-01)

CSS variables already defined in :root (from Phase 1 — do not redefine):
--rb-bg, --rb-surface, --rb-text, --rb-text-muted, --rb-accent, --rb-border,
--rb-font-body, --rb-font-heading
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add reader container and page visibility CSS</name>
  <files>assets/css/main.css</files>
  <action>
    Append the following CSS sections to the END of assets/css/main.css. Do not modify any existing rules. Add a clear comment header before each section.

    **Section 1: Reader wrapper and container (READER-08, READER-03)**
    ```css
    /* =====================================================
       E-BOOK READER — Container layout (Phase 2)
       READER-08: 100dvh container, READER-03: touch-action
    ===================================================== */

    .rb-bio-reader-wrap {
      display: flex;
      flex-direction: column;
      height: 100dvh;
      overflow: hidden;
    }

    .rb-bio-container {
      flex: 1;
      overflow: hidden;
      position: relative;
      touch-action: pan-y;           /* iOS Safari: signal horizontal swipe interest */
      -webkit-user-select: none;
      user-select: none;
    }
    ```

    **Section 2: Page visibility model (READER-01, LAYOUT-02)**
    ```css
    /* =====================================================
       E-BOOK READER — Page visibility model (Phase 2)
       READER-01: one page visible, LAYOUT-02: local scroll
    ===================================================== */

    /* Inactive pages: hidden from render pipeline, no layout cost */
    .rb-bio-container .rb-bio-page:not(.is-active) {
      content-visibility: hidden;
      contain-intrinsic-size: auto 800px;
      pointer-events: none;
      overflow: hidden;
    }

    /* Active page: full rendering + local vertical scroll for accordions */
    .rb-bio-container .rb-bio-page.is-active {
      content-visibility: auto;
      contain-intrinsic-size: auto 800px;
      overflow-y: auto;
      overscroll-behavior-y: contain;
      min-height: 0;
      flex: 1;
    }
    ```

    **Section 3: Controls bar (READER-04, READER-06)**
    ```css
    /* =====================================================
       E-BOOK READER — Controls bar (Phase 2)
       READER-04: prev/next buttons, READER-06: page indicator
    ===================================================== */

    .rb-controls-bar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 0.5rem;
      padding: 0.5rem 1rem;
      background: var(--rb-surface);
      border-top: 1px solid var(--rb-border);
      flex-shrink: 0;
    }

    .rb-ctrl-btn {
      background: transparent;
      border: 1px solid var(--rb-border);
      color: var(--rb-text);
      cursor: pointer;
      padding: 0.4rem 0.75rem;
      border-radius: 4px;
      font-size: 1rem;
      line-height: 1;
      min-width: 2.5rem;
    }

    .rb-ctrl-btn:hover {
      background: var(--rb-border);
    }

    .rb-ctrl-center {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.125rem;
      flex: 1;
    }

    .rb-page-indicator {
      font-size: 0.875rem;
      color: var(--rb-text);
      font-variant-numeric: tabular-nums;
    }

    .rb-year-badge {
      font-size: 0.75rem;
      color: var(--rb-text-muted);
    }
    ```

    **Section 4: TOC panel (NAV-03)**
    ```css
    /* =====================================================
       E-BOOK READER — TOC panel (Phase 2)
       NAV-03: year-structured table of contents
    ===================================================== */

    .rb-toc-panel {
      position: fixed;
      inset: 0;
      background: var(--rb-bg);
      z-index: 200;
      display: flex;
      flex-direction: column;
      overflow: hidden;
      /* Hidden state — toggled by JS adding .is-open */
      visibility: hidden;
      opacity: 0;
      transition: opacity 0.2s, visibility 0s 0.2s;
    }

    .rb-toc-panel.is-open {
      visibility: visible;
      opacity: 1;
      transition: opacity 0.2s, visibility 0s;
    }

    .rb-toc-inner {
      overflow-y: auto;
      overscroll-behavior: contain;
      padding: 1rem;
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }

    .rb-toc-close {
      align-self: flex-end;
      background: transparent;
      border: 1px solid var(--rb-border);
      color: var(--rb-text);
      cursor: pointer;
      padding: 0.4rem 0.75rem;
      border-radius: 4px;
      font-size: 1rem;
    }

    .rb-toc-list {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      gap: 0.25rem;
    }

    .rb-toc-item {
      width: 100%;
      text-align: left;
      background: transparent;
      border: 1px solid var(--rb-border);
      color: var(--rb-text);
      cursor: pointer;
      padding: 0.6rem 1rem;
      border-radius: 4px;
      font-size: 1rem;
    }

    .rb-toc-item:hover {
      background: var(--rb-surface);
    }
    ```

    **Section 5: Media-dedicated pages (LAYOUT-01)**
    ```css
    /* =====================================================
       E-BOOK READER — Media page layout (Phase 2)
       LAYOUT-01: dedicated page slot for video/image pages
    ===================================================== */

    .rb-bio-page--media {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
    }

    .rb-bio-page--media img,
    .rb-bio-page--media .rb-yt-embed {
      max-height: 80dvh;
      width: auto;
      max-width: 100%;
    }
    ```
  </action>
  <verify>
    <automated>grep -c "100dvh" /Users/hakanfilip/workspace/projects/private/borjlind/assets/css/main.css</automated>
  </verify>
  <done>
    - assets/css/main.css contains `.rb-bio-reader-wrap { height: 100dvh ... }`
    - `.rb-bio-container .rb-bio-page:not(.is-active) { content-visibility: hidden }` present
    - `.rb-bio-container .rb-bio-page.is-active { overflow-y: auto }` present
    - `.rb-controls-bar`, `.rb-ctrl-btn`, `.rb-page-indicator`, `.rb-year-badge` present
    - `.rb-toc-panel`, `.rb-toc-item` present
    - `.rb-bio-page--media` present
    - `touch-action: pan-y` on `.rb-bio-container` present
    - `grep -c "100dvh" assets/css/main.css` returns at least 2 (reader-wrap + media)
  </done>
</task>

</tasks>

<verification>
```bash
# READER-08: 100dvh on reader container
grep -n "100dvh" /Users/hakanfilip/workspace/projects/private/borjlind/assets/css/main.css

# READER-01: content-visibility model
grep -n "content-visibility" /Users/hakanfilip/workspace/projects/private/borjlind/assets/css/main.css

# READER-03: touch-action
grep -n "touch-action" /Users/hakanfilip/workspace/projects/private/borjlind/assets/css/main.css

# LAYOUT-02: overflow-y: auto on active page
grep -n "overflow-y: auto" /Users/hakanfilip/workspace/projects/private/borjlind/assets/css/main.css

# Build still works
cd /Users/hakanfilip/workspace/projects/private/borjlind && npx eleventy --dryrun 2>&1 | tail -3
```
</verification>

<success_criteria>
- `grep -n "100dvh" assets/css/main.css` matches `.rb-bio-reader-wrap`
- `grep -n "touch-action: pan-y" assets/css/main.css` matches `.rb-bio-container`
- `grep -n "content-visibility: hidden" assets/css/main.css` matches the `:not(.is-active)` rule
- `grep -n "overflow-y: auto" assets/css/main.css` matches `.rb-bio-page.is-active`
- `grep -n "overscroll-behavior-y: contain" assets/css/main.css` matches the active page rule
- `npx eleventy --dryrun` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-ebook-reader/02-03-SUMMARY.md`
</output>
