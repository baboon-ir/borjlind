---
phase: 03-design-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/main.css
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-02
  - DESIGN-03
  - DESIGN-04
  - DESIGN-05
  - DESIGN-06
  - DESIGN-07
must_haves:
  truths:
    - "Reader background and surface tokens are warm charcoal values (not pure #000000)"
    - "Reader text tokens are warm off-white values (not pure #ffffff) and maintain clear visual hierarchy with muted text"
    - "Reader prose base uses ~17px size, ~1.72 line-height, and subtle positive tracking"
    - "Reader prose/body uses a literary serif stack via `--rb-font-body` mapped into `.rb-page-prose` and `.rb-prose`"
    - "Reader measure is constrained around 65ch on desktop"
    - "Mobile prose insets are symmetric via logical properties (padding-inline), not asymmetric left/right hardcodes"
    - "Reader chrome (kicker/footer/TOC) uses the same palette family with understated contrast"
    - "No theme toggle UI or dual-theme token system is introduced"
  artifacts:
    - path: "assets/css/main.css"
      provides: "Phase 3 reading polish tokens and reader typography/chrome refinements"
      contains: "--rb-body-size"
  key_links:
    - from: "assets/css/main.css :root --rb-* tokens"
      to: ".rb-bio-container, .rb-page-prose, .rb-reader-kicker, .rb-reader-footer, .rb-toc-*"
      via: "single semantic token layer maps to prose and chrome components"
      pattern: "--rb-bg|--rb-text|--rb-body-size|--rb-measure"
---

<objective>
Apply visual/readability polish to the existing segment-based biography reader by refining palette, typography, spacing rhythm, measure, and chrome tone in `assets/css/main.css`.

Purpose: Complete all Phase 3 DESIGN requirements without changing navigation behavior or adding new reader features.
Output: Updated `assets/css/main.css` with production-ready reading tokens and component-level mappings for long-form comfort.
</objective>

<execution_context>
@/Users/hakanfilip/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hakanfilip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-design-polish/03-CONTEXT.md
@.planning/phases/03-design-polish/03-RESEARCH.md
@assets/css/main.css
@layouts/biography.njk
@includes/bio-toc.njk
</context>

<interfaces>
Required selector surfaces already in production:
- Reader shell: `.rb-bio-reader-wrap`, `.rb-bio-container`, `.rb-page-prose`, `.rb-reader-kicker`, `.rb-reader-footer`
- TOC: `.rb-toc-panel`, `.rb-toc-inner`, `.rb-toc-item`, `.rb-toc-item-page`, `.rb-toc-item-label`
- Typography container: `.rb-prose` and heading/paragraph descendants

Token direction locked by phase context:
- Warm charcoal backgrounds, warm off-white text, restrained muted hierarchy
- Body typography around 17px / 1.72 / +0.01em
- Prose measure near 65ch
- Single theme only
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Introduce explicit Phase 3 reader tokens</name>
  <files>assets/css/main.css</files>
  <action>
    In `:root`, add or refine reader semantic tokens for Phase 3 so typography and chrome can map to one source of truth. Keep existing token names where present; introduce missing variables as needed.

    Required token coverage:
    - Palette: `--rb-bg`, `--rb-surface`, `--rb-text`, `--rb-text-muted`, `--rb-border`
    - Typography rails: `--rb-font-body`, `--rb-body-size`, `--rb-body-leading`, `--rb-body-tracking`, `--rb-measure`
    - Inline spacing rails: `--rb-inline-inset-mobile`, `--rb-inline-inset-desktop`

    Constraints:
    - `--rb-bg` and `--rb-surface` must not be pure black.
    - `--rb-text` must not be pure white.
    - `--rb-font-body` must be a literary serif stack (book/editorial tone), not a generic default-like stack.
    - Keep `color-scheme: dark` and do not add light-theme alternative tokens.
  </action>
  <verify>
    <automated>rg -n "--rb-bg|--rb-text|--rb-font-body|--rb-body-size|--rb-body-leading|--rb-body-tracking|--rb-measure|--rb-inline-inset-mobile" /Users/hakanfilip/my-workspace/projects/borjlind/assets/css/main.css</automated>
  </verify>
  <done>
    - All required Phase 3 reader token variables exist in `:root`
    - Palette tokens are warm charcoal/off-white direction and avoid #000000/#ffffff extremes
    - `--rb-font-body` is defined as a literary serif stack suitable for long-form reading
    - Single-theme model remains intact
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply readability rails to prose measure, rhythm, and mobile insets + run static/build checks</name>
  <files>assets/css/main.css</files>
  <action>
    Update reader prose rules so long-form text follows the phase readability target.

    Required outcomes:
    - Reader prose max width uses `var(--rb-measure)` near 65ch.
    - Body reading style in `.rb-page-prose` and/or `.rb-prose` maps to:
      - `font-family: var(--rb-font-body)` (literary serif stack)
      - `font-size: var(--rb-body-size)` (~1.0625rem)
      - `line-height: var(--rb-body-leading)` (~1.72)
      - `letter-spacing: var(--rb-body-tracking)` (~0.01em)
    - Mobile inset rules are symmetric using logical properties (`padding-inline`) and shared inset tokens.
    - Remove or replace asymmetric paragraph/heading/figure padding patterns currently using different left vs right values.

    Preserve behavior:
    - Keep segment-scroll model unchanged.
    - Keep content selectors and markup contracts unchanged.

    Before completing this task, run:
    - `npm run build`
    - `rg -n "#000000|#ffffff" assets/css/main.css`
    - `rg -n "--rb-font-body|--rb-body-size|--rb-body-leading|--rb-body-tracking|--rb-measure" assets/css/main.css`
  </action>
  <verify>
    <automated>cd /Users/hakanfilip/my-workspace/projects/borjlind && npm run build && rg -n "max-width:\s*var\(--rb-measure\)|font-family:\s*var\(--rb-font-body\)|font-size:\s*var\(--rb-body-size\)|line-height:\s*var\(--rb-body-leading\)|letter-spacing:\s*var\(--rb-body-tracking\)|padding-inline" assets/css/main.css && rg -n "--rb-font-body|--rb-body-size|--rb-body-leading|--rb-body-tracking|--rb-measure" assets/css/main.css && rg -n "#000000|#ffffff" assets/css/main.css</automated>
  </verify>
  <done>
    - Prose measure is constrained around 65ch
    - Reader prose explicitly maps to `var(--rb-font-body)` literary serif stack
    - Reading type scale/leading/tracking is token-driven and meets phase targets
    - Mobile insets are symmetric and tokenized
    - `npm run build` exits 0
    - No pure black/white hardcoded extremes are used for primary reader background/body text tokens
  </done>
</task>

<task type="auto">
  <name>Task 3: Polish reader chrome tone (kicker/footer/TOC) using shared semantic tokens</name>
  <files>assets/css/main.css</files>
  <action>
    Refine `.rb-reader-kicker`, `.rb-reader-footer`, `.rb-toc-inner`, `.rb-toc-item`, `.rb-year-toggle`, and `.rb-page-indicator` so chrome stays visible but secondary to prose.

    Required outcomes:
    - Chrome surfaces use restrained contrast based on semantic tokens (`--rb-surface`, `--rb-border`, `--rb-text-muted`).
    - Active/interactive states remain legible but minimal.
    - Borders/backgrounds are visually consistent between top kicker, bottom footer, and TOC panel.
    - No new interactions or JS hooks are added.
  </action>
  <verify>
    <automated>rg -n "\.rb-reader-kicker|\.rb-reader-footer|\.rb-toc-inner|\.rb-toc-item|\.rb-year-toggle|\.rb-page-indicator|--rb-border|--rb-text-muted" /Users/hakanfilip/my-workspace/projects/borjlind/assets/css/main.css</automated>
  </verify>
  <done>
    - Chrome is visually integrated with the reading palette and remains understated
    - Existing TOC and footer behavior continue unchanged
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/hakanfilip/my-workspace/projects/borjlind
npm run build

# DESIGN-01 / DESIGN-02 guardrails
rg -n "#000000|#ffffff" assets/css/main.css

# DESIGN-04..07 rails
rg -n "--rb-font-body|--rb-body-size|--rb-body-leading|--rb-body-tracking|--rb-measure|padding-inline" assets/css/main.css
rg -n "font-family:\s*var\(--rb-font-body\)" assets/css/main.css
```
</verification>

<success_criteria>
- `assets/css/main.css` reflects warm-charcoal/off-white palette and single-theme approach
- Reader prose uses a literary serif stack with tokenized 17px/1.72/+tracking rails and ~65ch measure
- Mobile prose insets are symmetric and logical-property based
- Reader chrome tone is polished without behavior changes
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-polish/03-01-SUMMARY.md`
</output>
